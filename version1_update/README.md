# EBU6304 Group 98 — TA Recruitment System
**BUPT International School Teaching Assistant Recruitment System**
Iteration 2 (Intermediate Assessment — 12 April 2026)

---

## Quick Start

**Requirements:** Java JDK 8 or above (`javac` and `java` must be on PATH).

```
# 1. Compile
compile.bat

# 2. Run  (always from the project root)
run.bat
```

On first launch, the `data\` folder and four seed CSV files are created automatically.

---

## Demo Accounts

| Role  | Username | Password |
|-------|----------|----------|
| Admin | admin    | admin123 |
| TA    | ta1      | ta123    |
| TA    | ta2      | ta456    |
| MO    | mo1      | mo123    |
| MO    | mo2      | mo456    |

---

## Version1 Updates

Version1 introduces the following Swing-side improvements:

- **Logout flow on every dashboard** — TA, MO, and Admin dashboards now provide a logout confirmation and return to the login page so users can switch roles without restarting the app.
- **Clearer login validation** — the login page now tells the user whether the username is missing, the password is missing, the username does not exist, or the password is incorrect.
- **Expanded login examples** — the login screen now lists all seeded demo accounts directly for quick testing.
- **Editable Admin tables** — Admin overview tables now support inline editing.
- **Explicit Save / Undo flow for Admin editing** — Admin tables now stage edits in the UI first. Changes are written to CSV only after clicking **Save Changes**, while **Undo Changes** discards unsaved edits by reloading from CSV.
- **CSV sync for editable Admin data** — after clicking **Save Changes**, edits made in **All Applications** and **All Jobs** are written back to the corresponding CSV files. In **TA Workload Monitor**, identity fields (username / full name / email) are editable and synced, while workload totals remain derived from the latest application/job data.

### Version1 Admin Editing Notes

- **TA Workload Monitor**: editable columns are `TA Username`, `Full Name`, and `Email`.
- **TA Workload Monitor**: `Total Apps`, `Selected Jobs`, and `Total Hours` are computed values and are refreshed automatically.
- **All Applications**: editable changes are synced to `applications.csv`, and related job display fields are synced to `jobs.csv` when applicable, only after clicking **Save Changes**.
- **All Jobs**: editable changes are synced to `jobs.csv` only after clicking **Save Changes**.
- **Undo Changes**: reloads the latest CSV content and discards any unsaved cell edits in the current Admin table.
- Invalid edits (for example, unknown MO username or non-numeric Max Hours) are rejected and are not saved.

---

## Features Implemented (Iteration 1 + 2)

| User Story | Feature | Done |
|---|---|---|
| US-1 | TA creates / edits personal profile | ✔ |
| US-2 | TA records CV file path | ✔ |
| US-3 | TA browses open jobs | ✔ |
| US-4 | TA applies for a job | ✔ |
| US-5 | MO posts a new job | ✔ |
| US-6 | MO views applicants and selects / rejects them | ✔ |
| —    | TA checks application status (colour-coded) | ✔ |
| —    | Admin monitors TA workload (highlights overloaded TAs) | ✔ |

---

## Project Structure

```
EBU6304_Group98-dev_dengbowen/
├── src/                    Java source files (default package, no build tool needed)
│   ├── Main.java           Entry point — initialises data files, launches LoginFrame
│   ├── User.java           Model: user account (id, username, password, role)
│   ├── TAProfile.java      Model: TA personal profile
│   ├── Job.java            Model: job posting
│   ├── Application.java    Model: job application
│   ├── FileStorage.java    Utility: all CSV read / write logic + first-run seed data
│   ├── LoginFrame.java     UI: login screen (routes to correct dashboard by role)
│   ├── TADashboard.java    UI: TA tabs — Profile / Browse Jobs / My Applications
│   ├── MODashboard.java    UI: MO tabs — Post Job / My Job Posts / View Applicants
│   └── AdminDashboard.java UI: Admin tabs — Workload Monitor / All Applications / All Jobs
├── bin/                    Compiled .class files (generated by compile.bat)
├── data/                   CSV data files (auto-created on first run)
│   ├── users.csv
│   ├── profiles.csv
│   ├── jobs.csv
│   └── applications.csv
├── compile.bat             Windows compile script
├── run.bat                 Windows run script
└── README.md               This file
```

---

## Data Format

All data is stored as plain CSV files in `data\`. No database is used.

| File | Columns |
|---|---|
| `users.csv` | id, username, password, role |
| `profiles.csv` | id, userId, fullName, email, studentId, skills, gpa, cvPath |
| `jobs.csv` | id, moId, title, module, description, requiredSkills, maxHours, status |
| `applications.csv` | id, taId, jobId, status, appliedAt |

> **Note:** commas inside field values are automatically replaced with semicolons by
> `FileStorage.sanitise()` to preserve CSV integrity.

---

## For the Next Developer — What Still Needs to Be Done

Upcoming iterations (3 & 4) should add:

- [ ] **US-7** Admin workload monitor (iteration 3) — basic version is already in AdminDashboard; extend with export/filter options.
- [ ] **US-8** TA application status — already visible; consider adding push notifications or email alerts.
- [ ] **US-9 / US-10** AI skill matching & workload balancing (iteration 4).
  - Suggested approach: simple keyword overlap between `job.requiredSkills` and `taProfile.skills` gives a matching percentage — no external library needed.
- [ ] **UI polish** — add sorting/filtering to tables, improve layout for smaller screens.
- [ ] **Input validation** — email format check, GPA range (0.0–4.0), duplicate username prevention on registration.
- [ ] **Registration screen** — currently only seeded accounts can log in; add a "Register" button on `LoginFrame`.
- [ ] **JavaDoc** — add `/** */` comments to all public methods for the final submission.
- [ ] **Unit tests** — write JUnit tests for `FileStorage` CRUD methods.

### Next Technical Update After Version1

- [ ] Add stricter validation rules for Admin inline editing (email format, status whitelist, timestamp format).
- [ ] Add close-window / tab-switch prompts when there are unsaved Admin edits.
- [ ] Add editable admin controls for more source-of-truth data instead of exposing derived summary columns.

### How to Add a New Feature (example: registration)

1. Add a "Register" button to `LoginFrame.java` → open a new `RegisterFrame.java`.
2. In `RegisterFrame`, collect username/password/role, call `FileStorage.loadUsers()`, check for duplicates, append the new `User`, and call `FileStorage.saveUsers()`.
3. That's it — no schema migration needed because the CSV is plain text.

### Coding Conventions Used

- **No packages** — all source files live in `src\` with the default package for easy `javac` compilation without a build tool.
- **No external libraries** — only the Java standard library (`javax.swing`, `java.io`, `java.time`).
- **CSV delimiter** — comma (`,`); field values must not contain raw commas (use `FileStorage.sanitise()`).
- **Skill lists** — stored as semicolon-separated strings (e.g. `Java;OOP;Git`).

---

*Last updated: Iteration 2 — 4 April 2026*
